tricks:
  - watchdog.tricks.AutoRestartTrick:
      command: ["python", "-m", "wbld.bot"]
      patterns:
        - "*.py"
  - watchdog.tricks.AutoRestartTrick:
      command: ["python", "-m", "wbld.web"]
      patterns:
        - "*.py"
        - "*.html"
        - "*.jinja2"
        - "*.css"
